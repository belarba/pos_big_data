"""Training a model (`train.py`) and using it (`predict.py`).

This file makes `modeling` a regular package inside `src`, so the two files are
imported as `src.modeling.train` and `src.modeling.predict`. Folders whose files
are only run, such as `notebooks`, do not need one.
"""
